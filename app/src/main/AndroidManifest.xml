<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.doudou.cn.shoppingim" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>

    <!-- 可选 -->
    <uses-permission android:name="android.permission.GET_TASKS"></uses-permission>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="com.doudou.cn.shoppingim.application.MyApplication"
        android:theme="@style/AppTheme"
       >

        <!-- 欢迎界面-->
        <activity
            android:name="com.doudou.cn.shoppingim.ui.WelcomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--登陆界面-->
        <activity android:name="com.doudou.cn.shoppingim.ui.LoginActivity"/>
        <!-- 主界面-->
        <activity android:name="com.doudou.cn.shoppingim.ui.MainActivity"/>

        <!-- bugHg 的key 设置-->
        <meta-data android:value="09489de8e24847fca31caa428d6b5ff6" android:name="BUG_HD_SDK_GENERAL_KEY" />
    </application>

</manifest>
